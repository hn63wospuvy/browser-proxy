# Rust
/target
**/*.rs.bk
Cargo.lock

# Node (only used by the asset-vendoring script)
node_modules/
scripts/node_modules/
scripts/package-lock.json

# Local route config + cached WARP registration credentials
/config.yaml
/warp-*.json

# OS
Thumbs.db
.DS_Store
